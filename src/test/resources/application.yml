spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/shiftdatabase
    username: admin
    password: shift11!!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect